# Pending audited improvements — 15 September 2026

This release packages the pending customer-approved batch from both
`codex/late-payment-settings-20260914` worktrees. It preserves remote main
ancestry and uses the protected production deployment workflows.

## Scope

- Shared guard/security-supervisor team definitions across property categories;
  existing assignment scopes and fixed permission templates remain enforced.
- Marketplace common-KYC reuse, supplemental registered-rider verification,
  referee management, variations/stock, payment-destination snapshots and
  phone-friendly assigned-delivery actions.
- Business notifications, accurate unread counts, paired channel records,
  private-code exclusion, current invitation actions and TextSMS reconciliation.
- Working reports/navigation/export with selected-workspace financial scoping
  and no additional role permissions.
- Wealth navigation, owner-scoped goals and safe original-currency writes.
- Help Desk source-grounded structured answers, current role-visible knowledge,
  human escalation, readable references and the packaged user manual.

Detailed findings and limits are in the adjacent module audit documents.

## Database and deployment

Production was verified as V82 with no failed Flyway records before packaging.
Candidate adds V83 shared team definitions, V84 rider credentials, V85 Soko
order destination snapshots and V86 durable notification/receipt metadata.
V85's legacy backfill freezes the currently configured store receiving account;
it is not evidence of the historical payment's actual recipient. Existing
invoice/payment records remain authoritative. No financial status is fabricated.

The release-only Flyway test uses the existing data-free schema fixture. Its
legacy `production-v80-schema.sql` filename is misleading: inspected V81 tables
and V82 conditional-rule/delivery-recovery columns establish its V82 baseline.
The initial V80-baseline attempt correctly failed on existing V81 tables;
the V83–V86 rehearsal uses a fresh explicitly named loopback disposable database.
A fresh production-schema export was blocked by safety review and
was not performed. This rehearsal is not a claim of a fresh production clone.

The deployment workflow requires dependency preflight before and after,
host-local protected primary/audit database backup archives and checksums,
clean immutable build revisions, rollback JAR/front-end directories, readiness
and deployed-revision verification. Database recovery is forward-only unless
an explicit recovery operation is separately authorised. Credentials remain
in their existing protected locations; none are packaged in source archives.

## Verification and remaining boundaries

The frozen application source passed the full backend unit suite: 1,019 tests,
zero failures/errors, one existing application-context skip. Help Desk browser
tests passed locally; the full browser/build/deployment gates run for this release.
Do not interpret API-mocked browser tests as live multi-role/provider certification.

Outstanding wider business decisions and work remain documented in
`pending-audit-completion-2026-09-15.md` and the module audits: provider-confirmed
refund/reversal cleanup, perishable returns, independent service assets/custom
KYC mapping, wealth closed-account semantics and live factual AI evaluation.
This release does not enable retrospective reminder blasts, automated refunds,
escrow, additional permissions, or new WhatsApp/Maps data-sharing behaviour.

The manual remains governed draft knowledge; deployment does not overwrite
published production help articles. Review/publish updated articles separately.

Final immutable revisions, artifact checksums and workflow results are reported
in the release handoff after the protected workflows finish.
